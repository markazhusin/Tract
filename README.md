# Tract

Локальный-first P2P messenger с входом по логину и паролю.

## Что есть

1. Регистрация по логину вида `@login` и паролю.
2. Автоматический вход после перезагрузки приложения, пока пользователь явно не нажал `Выйти`.
3. Единое пространство без пользовательских комнат.
4. Поиск и добавление контакта по `@login`.
5. Пользователи не появляются в списках автоматически: чат создается только после добавления по логину или входящего сообщения.
6. Локальная история сообщений и контактов в `IndexedDB`.
7. Удаление выбранных сообщений у себя или у всех.
8. Очистка истории чата у себя или у всех.
9. P2P-доставка через WebRTC и fallback через signaling relay.

## Запуск

```bash
npm install
npm start
```

Сервер отдаёт собранный frontend из `dist` и signaling host на `http://127.0.0.1:8877`.

Для разработки:

```bash
npm run dev
```

## Как пользоваться

1. Создайте аккаунт с логином `@login` и паролем.
2. Передайте другу ссылку на приложение.
3. Найдите друга по его `@login` в поиске над чатами или на вкладке контактов.
4. Откройте чат и отправьте сообщение.

Сервер не хранит приватные ключи и историю сообщений. Он нужен для presence, поиска онлайн-peer по логину, WebRTC handshake и очереди signaling-сообщений.

## Деплой на Railway (GitHub)

В репозитории уже есть `railway.json`:

- **build:** `npm run build` — собирает `dist/` (включая PWA: `sw.js`, `manifest.webmanifest`, иконки из `public/`)
- **start:** `npm start` — `signaling-server.js` слушает `PORT` и отдаёт статику из `dist/`

После пуша в GitHub Railway сам собирает и поднимает сервис. Домен Railway уже с **HTTPS** — для PWA, уведомлений и счётчика на иконке этого достаточно.

1. Закоммитьте и запушьте изменения (в т.ч. папку `public/` и `app/pwa.js`).
2. Дождитесь успешного деплоя в Railway.
3. Откройте ваш `*.up.railway.app` (или свой домен).
4. В **Настройках** включите уведомления и разрешите их в браузере.
5. На телефоне: «Добавить на главный экран» / «Установить приложение».

Signaling URL подставляется с того же origin, что и страница — отдельный порт или `?signal=` для Railway не нужны.

**Важно:** push-уведомления срабатывают, пока вкладка или установленное PWA ещё работает в фоне. Если приложение полностью закрыто, без push-сервера уведомления не придут.

### Офлайн-сообщения

Текстовые сообщения и сигналы звонка сохраняются на сервере в `data/message-inbox.json` по логину `@user` (не по сессии peer). Получатель увидит их после входа — даже через сутки и после выхода на всех устройствах.

На Railway для сохранения между деплоями подключите volume к `/data` (или каталогу проекта с `data/`).

### Safari и звонки

В **Safari** (вкладка, не закрытая) звонки идут через серверный relay и опрос — вкладка должна быть открыта. На iPhone надёжнее **«На экран Домой»** (PWA). Если звонок не пришёл: откройте Tract, подождите 1–2 сек (подтянется inbox).
